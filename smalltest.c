int func(int a, int b) {
    return 1;
}

int main(int argc, char** argv) {
    int x;
    int y;
    char z;
    char w;
    int* a;
    int f[10];

    y * x;

    x && y;

    x == y;
    x != y;
    w == z;
    w == x;

    y / x;
    y % x;
    y < x;
    y > x;
    w <= z;
    w >= x;

    w % x / z;

    x - 3;
    y + 1;
    y == "asdf";
    y[13];
    f[x];

    -y;
    !y;
    sizeof y;
    *a;
    &y;
    func(x, y);
}
